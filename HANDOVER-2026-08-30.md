# Handover — 2026-08-30 (night)

Production is **live and stable at `05b89f9b`** (AWS 65.0.27.161). 10 Mahavidya
agents ACTIVE · API health 200 · isolation health 0/0/0. Nothing is mid-flight.
Tomorrow starts from here.

---

## What shipped today (8 releases, all safe-deploy verified)

| Area | Commit | State |
|---|---|---|
| Strict role lock at the API guard (external roles confined) | d7e022e | live |
| Per-document KYC ownership lock + PDF `openDocument` view fix | d7e022e | live |
| Migration 110 — `company_id` on trips + bills, MIXED_COMPANY guard, IOCL backfill | 7426bce | live · **1020 trips + 417 bills, 0 NULL, 0 orphans** |
| BHUVANESHWARI (OCR agent) wired — `POST /files` emits `document.uploaded` | 22ae3ec / 1994b95 | live · proven 0→1 run |
| One-way AWS→Local backup script | cb3660a | script ready · **first backup 266 MB done** (49 MB DB dump + 1601 PDFs) at `F:\Prasad_Transport_System\ERP-LOCAL-BACKUP\` |
| 2026 Operations **Command Deck** as the Operations Home (`OPS_DECK`) | a31eb1d / 49895cb | live · zero widget loss, Master Control v5.0 intact |
| Ledger batch — `company_id` on trip-linked postings + cash pass-through | 05b89f9b | live · 7 sites |

Full detail: `reports/DATA-ISOLATION-AUDIT-2026-08-30.md`.

---

## Tomorrow's tasks (in order)

### 1. Port the **Accounts** module to the 2026 design
- Mirror the pattern used for Operations: a new `AccountsDeck2026.tsx` as the
  Accounts **Home**, additive — **remove nothing**. Preserve every existing tool
  (Ledger & Cash Book, P&L, Balance Sheet, Billing, Loan/EMI, GST/TDS, Fleet Card,
  Customer/Owner statements) re-styled in the deep slate-navy / signal-amber theme.
- Data source: `GET /api/v1/dashboard/v5` `finance:{...}` block (money spread onto
  `finance` directly — freight_income, received, unbilled_freight; plus `banks`,
  `groups`, `pnl`, `health`, `emi`, `toll`, `monthly`, `ledger_book`). Also
  `GET /api/v1/finance/dashboard` and `/finance/reports/*`.
- Wire like Operations: add `ACCT_DECK` to the always-allowed lists in
  `App.tsx` (line ~328) and `SIDEBAR.tsx` (line ~77), a render `case`, a sidebar
  item at the top of the ACCOUNTS group, and land ACCOUNTS on it in
  `handleModuleChange` / initial `activeComponent` (currently ACCOUNTS→MASTER_CONTROL_V5).
- **Smoke-test the live v5 `finance` shape first** (temp auth_sessions row trick —
  see below) so no KPI reads `undefined`, the way the Operations `unbilled_freight`
  bug was caught before the user saw it.

### 2. Remaining ledger-mapping sites — needs a **business decision first**
The trip-linked and cash sites are done. Still NULL-scoped, deliberately:
`loan EMI`, `maintenance`, `tyre`, `toll recharge/cards` (assets.routes.js,
toll.routes.js, loanImport.routes.js). These need **firm attribution rules**,
not a code default:
- **Loans:** map each `loan_master` (or its `financier_ledger`) → one operating
  company. Ask the owner which firm each financier/loan belongs to, then stamp.
- **Toll/FASTag:** the per-firm wallet already exists (`FASTag Wallet: Jaiswal`
  / `: Prasad`) — derive `company_id` from the wallet the recharge/txn hits.
- **Maintenance/tyre:** for an OWNED truck use its firm; for an ATTACHED truck it
  debits the owner khata (already), so company_id can stay NULL there.
Once decided, they carry `company_id` into `postVoucher`; the schemas already
forward it via `postJournal`. They surface on `v_accounting_health` until then.

### 3. Schedule the daily one-way AWS→Local backup
- Script: `scripts/backup-aws-to-local.sh` (pulls both upload roots + a `pg_dump`,
  prunes >30 days, never writes back — production stays sole writer per
  `ERP_API.KILL`).
- Windows Task Scheduler, daily (suggest ~02:00):
  `"C:\Program Files\Git\bin\bash.exe" -lc "/f/Prasad_Transport_System/PRASAD-TRANSPORT-ERP/scripts/backup-aws-to-local.sh"`
- Verify one scheduled run lands in `F:\Prasad_Transport_System\ERP-LOCAL-BACKUP\`.

---

## Working notes for tomorrow (so we start fast)

- **Deploy:** commit → `npm run ship:release` (fast-forwards `main`; box cron pulls
  in ~3 min). Done only when `.last-deployed-sha` on the box == the new sha.
  Frontend build is `vite build` (no typecheck; `@ts-nocheck` on big screens).
- **Verify a live API endpoint's real JSON shape** (needs a session, not just a
  signed token): insert a 2-min `auth_sessions` row for a minted jti against an
  ADMIN user, call the endpoint, delete the row. Pattern used today in
  `/tmp/dumpv5c.mjs` on the box (JWT_SECRET lives in `.env`, not `.env.api`).
- **Do NOT** enable two-way DB sync (split-brain — `ERP_API.KILL`). One-way only.
- **Do NOT** touch Load Bazaar / bidding — it is Phase-2 backlog by owner's call.
- Repo: `F:\Prasad_Transport_System\PRASAD-TRANSPORT-ERP` (branch `upgrade-2026`,
  releases to `main`). The `F:\PRASAD-TRANSPORT-TERMINALS` cwd is just a landing.
